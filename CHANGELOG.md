# 更新日志

本项目从 `v1.3.0` 开始记录自定义版本。`v1.0`、`v1.1` 和 `v1.2`
是仓库继承的上游历史标签，不包含当前完整的自定义功能。

## 未发布

## [v2.3.1] - 2026-08-19

### 新增：自动标注置信度调节

- 在上方工具栏的模型标注按钮旁增加置信度数值框，范围 `0.01–1.00`，默认 `0.25`，步长 `0.05`。
- 批量“自动标注”和“标注当前图”共用当前数值，并把它直接传给 Ultralytics `conf` 参数。
- 修改后的置信度保存在本机设置中并跨重启恢复；非法旧值自动回退到 `0.25`。
- 推理期间锁定数值框，状态栏和完成窗口显示本次实际使用的置信度。

## [v2.3.0] - 2026-08-19

### 新增：当前图片空标签

- 上方工具栏增加“生成空标签”，按当前 `Annotation Format` 为正在查看的图片立即生成空标签。
- Pascal VOC 模式生成无 object 的 XML；YOLO 与 YOLO OBB 模式生成零字节 TXT。
- 当前图片已有框时先询问是否清空，取消不会修改数据；保存失败会自动恢复原框。
- 成功后右侧文件列表显示 `[BG]`，并可通过 `Ctrl+Z` 恢复清空前的框。

### 新增：自定义标签快捷键

- 在右侧 `Box Labels` 中增加“标签快捷键设置”，可配置多个“按键 → 预设类别”映射。
- 目标类别只能从启动时加载的 `class.txt` 中选择，不能写入临时或未知类别。
- 保存前会检查重复映射、菜单/工具栏快捷键及画布操作键，存在冲突时拒绝保存。
- 按下自定义快捷键会选择对应默认类别并直接进入一次 OBB 绘制；完成后不再弹出类别选择器。
- 映射保存在本机设置中并跨重启恢复；类别编辑器打开时会暂时禁用，避免输入时误触。

### 新增：标签数量统计面板

- 在右侧 `Box Labels` 下方显示项目总标签数、当前图片标签数和本次工作已打标签数。
- 项目与当前图片数量会随新建、复制粘贴、删除、撤销、保存及模型标注实时更新。
- 本次工作数量只统计当前程序运行期间仍然存在的新增框；删除本次新增框时同步减少，删除旧框不会误减，撤销删除后恢复。
- 会话来源标记只保存在运行内存中，不写入 XML/TXT，重新启动后计数归零。
- 复用右侧文件列表已有的标签数量缓存，不会因每次修改都重新扫描全部标签文件。

### 改进：切换格式时转换已有标签

- 手动选择新的 `Annotation Format` 时，自动扫描当前图片数据集对应的全部标签。
- 已有标签会批量转换成 Pascal VOC XML、YOLO 或 YOLO OBB，并显示文件级进度条。
- 每个目标文件安全写入成功后才移除旧格式；失败文件保留原标签并在完成窗口列出。
- YOLO 与 YOLO OBB 共用 `.txt` 扩展名，切换时会自动把 5 列与 9 列内容互相转换。
- 没有标签时直接切换后续保存格式并提示修改成功，不显示无意义的进度条。
- 模型自动识别输出类型时只改变模型输出格式，不触发已有人工标签的批量转换。

### 移除：Export to

- 删除 `File > Export to` 菜单以及旧的 XML 单向导出实现。
- 统一使用 `File > Annotation Format` 完成 XML、YOLO、YOLO OBB 双向批量转换。

### 改进：默认开启自动保存

- 新用户首次启动时，`View > Auto Saving` 默认勾选，切换图片会自动保存当前标签。
- 用户手动关闭自动保存后，后续启动仍会尊重已保存的关闭状态。

### 新增：剪切 Box

- 新增标准快捷键 `Ctrl+X`，可一次剪切当前选中的一个或多个 Box。
- 剪切后的第一次粘贴保留类别、大小、角度和原始坐标；也可切换图片后原位置粘贴。
- 剪切和粘贴分别记为一步操作，可使用 `Ctrl+Z` 逐步撤销。
- 新增多框剪切、原位置粘贴和撤销回归测试。

### 改进：单张模型标注可撤销

- 单张自动标注的覆盖或追加结果现在会作为一条独立的 `Ctrl+Z` 撤销记录。
- 模型推理前已有的撤销历史会在图片重新加载后继续保留。
- 推理失败或没有改变标签时不会增加空撤销记录。
- 撤销后按 `Ctrl+S` 或使用自动保存，将恢复结果写回标签文件。
- 新增覆盖、追加和失败场景的撤销回归测试。

## [v2.2.0] - 2026-08-11

### 新增：框操作撤销

- 新增标准快捷键 `Ctrl+Z`，保留 `Ctrl+V` 作为粘贴。
- 支持撤销框移动、粘贴、删除、复制、缩放、旋转以及类别修改等操作。
- 一次连续拖动合并为一步；每张图片独立保留最多 50 步，切换图片后自动清空。
- 新增移动、粘贴、多框删除和连续撤销自动化测试。

### 修复：自动标注类别匹配

- 模型类别与项目预设完全同名时，强制优先映射到该同名类别。
- 修复共享单词被错误计算为 `100%` 相似的问题，例如不再把模型的 `pipe_row` 映射成 `Drill_pipe`。
- 新增当前全部预设类别必须映射回自身的回归测试。

## [v2.1.0] - 2026-08-11

### 新增：当前图片自动标注

- 顶部工具栏和 `File` 菜单新增“标注当前图”。
- 只对当前打开的图片执行一次本地 YOLO / YOLO OBB 模型推理。
- 当前图片已有标签时，明确询问“覆盖原标签”“直接添加”或“取消”。
- 覆盖模式只保存模型新框；追加模式保留当前框并加入模型新框。
- 继续自动识别普通检测与 OBB 模型、切换 YOLO 保存格式并执行类别模糊匹配。
- 单张任务沿用状态栏进度和中止按钮，完成后自动重新加载当前图片。
- 新增覆盖、追加、后台保存和工具栏入口自动化测试。

## [v2.0.2] - 2026-08-11

### 独立仓库发布

- GitHub 仓库正式脱离上游 Fork 网络，作为独立维护的派生项目发布。
- 完整保留上游提交历史、来源说明、原作者版权和 MIT 许可证文本。
- 项目整体继续采用 GNU AGPL v3.0 免费开源，不改变 v2.0.1 的功能行为。
- README、NOTICE、下载链接和软件版本号统一更新为 v2.0.2。

## [v2.0.1] - 2026-08-11

### 许可证合规调整

- 当前组合发行版的整体许可证由 MIT 调整为 GNU AGPL v3.0，以匹配
  Ultralytics 自动标注依赖的开源许可要求。
- 根目录 `LICENSE` 替换为完整 GNU AGPL v3.0 许可证文本。
- 上游 LabelImg2 的原始 MIT 许可证和版权声明原样保存在
  `LICENSE-MIT-UPSTREAM`，不改变上游代码原有权利和归属。
- README、NOTICE、安装教程、软件版本号和打包元数据统一更新。
- 此版本只调整许可证和发布说明，不改变 `v2.0.0` 的自动标注行为。

## [v2.0.0] - 2026-08-11

### 新增：本地模型自动标注

- 工具栏和 `File` 菜单增加“自动标注”。
- 支持加载本地 Ultralytics YOLO 检测或 YOLO OBB `.pt` 模型。
- 模型在后台线程逐张推理，界面状态栏显示文件名、框数量和总体进度。
- 状态栏提供“中止”按钮；中止请求会在当前图片推理完成后生效。
- 自动识别模型任务：普通检测模型保存为 5 列 YOLO，OBB 模型自动切换并保存为
  9 列 YOLO OBB。
- 使用模糊匹配把模型类别映射到项目类别预设，例如 `apple -> apples`、
  `org -> oranges_`、`melon -> name_melon`。
- 完成窗口显示模型类别到项目类别的映射和相似度，方便人工复核。
- 自动标注保留图片目录的相对子目录结构，并写入当前 `Open Annotation Dir`。
- 已存在 XML 或 TXT 标签的图片自动跳过，不覆盖人工标注。
- `.pt` 文件加入 Git 忽略列表；仓库和发行源码包不包含任何第三方模型权重。
- 增加 `ultralytics>=8.3` 运行依赖；未安装时不影响手工标注界面启动。

### 验证

- 通过类别模糊匹配、YOLO OBB 四点坐标转换、后台批量保存、标签重新载入、
  已有标签保护和中止流程测试。
- 通过 Python 语法编译及 Conda 启动器检查。

## [v1.5.0] - 2026-08-10

- 重写 Windows 启动器，支持项目 `.venv`、名为 `labelimg2` 的 Conda 环境和
  当前激活的 Conda 环境。
- 增加 `--venv`、`--conda` 和 `--check` 参数，各环境互不回退和污染。
- 移除对 Windows `py/pyw` 注册表启动路径的依赖。
- 完善中文小白安装教程和首次运行检查。

## [v1.4.0] - 2026-08-10

- 支持直接选择保存 Pascal VOC XML、普通 YOLO 或 YOLO OBB。
- 合并标签读取和保存目录入口为 `Open Annotation Dir`。
- 自动识别 5 列 YOLO 与 9 列 YOLO OBB TXT。
- 支持图片和标签按相同子目录结构递归匹配。
- XML 导出只转换同名 TXT，不复制图片、不划分数据集、不生成 YAML。

## [v1.3.0] - 2026-08-09

- 建立以 OBB 为主的快捷标注流程。
- 增加绘制状态切换、画完自动选择类别、常用类别优先、角度快捷键等功能。
- 增加框复制粘贴、多选、集体移动、滚轮缩放框和跨图片原位置粘贴。
- 增加自然数字排序、目录与标注进度恢复。

## 历史上游标签

- `v1.2`：上游历史版本。
- `v1.1`：上游历史版本。
- `v1.0`：上游历史版本。

[v2.3.1]: https://github.com/auto-sun/labelImg2-custom/releases/tag/v2.3.1
[v2.3.0]: https://github.com/auto-sun/labelImg2-custom/releases/tag/v2.3.0
[v2.2.0]: https://github.com/auto-sun/labelImg2-custom/releases/tag/v2.2.0
[v2.1.0]: https://github.com/auto-sun/labelImg2-custom/releases/tag/v2.1.0
[v2.0.2]: https://github.com/auto-sun/labelImg2-custom/releases/tag/v2.0.2
[v2.0.1]: https://github.com/auto-sun/labelImg2-custom/releases/tag/v2.0.1
[v2.0.0]: https://github.com/auto-sun/labelImg2-custom/releases/tag/v2.0.0
[v1.5.0]: https://github.com/auto-sun/labelImg2-custom/archive/refs/tags/v1.5.0.zip
[v1.4.0]: https://github.com/auto-sun/labelImg2-custom/archive/refs/tags/v1.4.0.zip
[v1.3.0]: https://github.com/auto-sun/labelImg2-custom/archive/refs/tags/v1.3.0.zip
