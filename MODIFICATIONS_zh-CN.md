# 本派生项目的功能改动

本文档总结相对于上游 `chinakook/labelImg2` 的主要变化。

完整的分版本记录见：[更新日志](CHANGELOG.md)。

## 本地模型自动标注

- 支持选择本地 Ultralytics YOLO 或 YOLO OBB `.pt` 模型。
- 自动识别普通检测与 OBB 任务，并切换为对应的 YOLO 标签保存格式。
- 模型类别通过单复数、分隔符、包含关系和字符串相似度匹配到项目类别预设。
- 完整同名类别强制优先于共享单词和其他模糊候选，避免 `pipe_row` 错配为 `Drill_pipe`。
- 后台逐张推理，状态栏显示进度、当前图片和框数量，并提供中止按钮。
- 按图片相对目录写入 `Open Annotation Dir`；已有 XML/TXT 自动跳过。
- 顶部工具栏可只标注当前图片；已有标签时可选择覆盖、直接添加或取消。
- 单张自动标注结果作为一条 `Ctrl+Z` 历史记录，可整体撤销并重新保存。
- 完成后显示类别映射和相似度，便于人工复核。

## 更便捷的 OBB 标注流程

- `E` 可以进入或退出旋转框绘制状态。
- 取消容易误触的 `W` 普通框快捷键。
- 每画完一个框自动返回编辑状态，不会不停进入下一个绘制状态。
- 新框自动选中并打开类别编辑器，可直接输入类别首字母。
- 类别编辑期间禁用图片切换快捷键，防止输入 `d` 时误切图片。

## 更快的类别选择

- `set as default` 设置的类别可以跨重启保存。
- 相同首字母的类别按照使用频率和最近使用顺序排列。
- 1.2 秒内连续按同一个首字母，可以在匹配类别中循环。
- 类别使用统计保存在本机设置文件，不会写入标注数据。

## 更统一的鼠标操作

- 没有选中框时，滚轮缩放图片。
- 有选中框时，滚轮缩放选中框，不会同时缩放图片。
- `Alt + 左键拖动`平移画布。
- 图片空白处拖动可以框选多个 Box。
- `Ctrl/Shift + 框选`可以追加选择。
- 多个选中框可以集体移动、缩放、复制和删除。

## 复制粘贴和重复目标标注

- 支持 `Ctrl+C`、`Ctrl+X`、`Ctrl+V` 和右键复制粘贴；剪切可作用于多个选中框。
- 支持 `Ctrl+D`批量复制选中框。
- 支持 `Ctrl + 拖动 Box`直接复制并移动。
- 同图粘贴自动偏移，避免框完全重叠。
- 剪切后的第一次粘贴保留原坐标，剪切和粘贴均可分别撤销。
- 跨图片粘贴保留原坐标、类别、大小和角度。
- `Ctrl+Z` 可逐步撤销当前图片的移动、粘贴、删除、缩放、旋转和类别修改等操作。
- 一次连续拖动只占一条撤销记录；每张图片最多保留 50 步，切换图片时自动清空。

## 图片浏览和进度恢复

- `A/D` 和左右方向键切换图片。
- 图片按照自然数字顺序排列。
- `View > Auto Saving` 首次启动默认开启；之后会记住用户手动选择。
- 自动恢复上次图片目录和具体标注位置。
- 自动恢复标签目录、保存格式、默认类别和类别使用顺序。
- Change Dir 不再覆盖用户明确选择的标签保存目录。

## 保存和稳定性

- 保存任一标签格式前自动创建缺少的相对子目录。
- 图片读取失败时显示错误，不继续使用无效画布。
- 空 Pixmap、无效尺寸和异常缩放值增加保护。

## 标签格式和标签目录

- 每张图片可以直接选择保存为 Pascal VOC XML、普通 YOLO 或 YOLO OBB。
- `Change Save Dir` 和单文件 `Open Annotation` 合并为 `Open Annotation Dir`，同一个目录同时负责读取和保存。
- XML 和 TXT 都按图片目录的相对子目录结构递归匹配。
- 自动识别 5 列普通 YOLO 和 9 列 YOLO OBB。
- 优先读取当前选择的保存类型；对应文件不存在时，另一种作为后备。
- 右侧文件列表统计 XML 和两种 TXT 的标签数量，空文件显示为背景图。
- 普通 YOLO 保存旋转框时使用轴对齐外接矩形；YOLO OBB 保留四顶点。
- 类别 ID 按当前类别预设顺序映射。

## YOLO/YOLO OBB 导出

- XML 直接转换为同名 YOLO 或 YOLO OBB `.txt`。
- 普通 YOLO 使用中心点和宽高格式。
- YOLO OBB 使用归一化四顶点格式。
- 不复制图片。
- 不重复生成 train 和 val 数据。
- 不创建数据集划分目录。
- 不生成 YAML、列表或其他附加文件。
- 类别 ID 按当前类别预设顺序确定。

## 启动、稳定性和文档

- 未开启自动保存时，带有未保存修改的图片在切换前会弹出确认，不再静默丢失框。
- 修正滚轮缩放时传入 Qt 控件的浮点数，避免新版 Python/PyQt5 下直接退出。
- `labelImg.bat` 同时支持当前激活的 Conda、项目 `.venv` 和名为 `labelimg2` 的 Conda 环境，并彻底取消 Windows `py/pyw` 注册表依赖。
- `--venv` 和 `--conda` 可以强制选择一种环境且不会回退；`--check` 只检查并显示选中的环境。
- `Paint Labels` 改用 `Ctrl+Shift+L`，不再与 `Play` 的 `Ctrl+Shift+P` 冲突。
- GitHub 使用中文 `README.md` 作为主页，同时保留英文 `README.rst` 入口。
- 增加独立中文小白教程，覆盖下载、安装、类别文件、标注与格式转换。

## 许可证

- 从 v2.0.1 开始，当前组合发行版整体采用 GNU AGPL v3.0 免费开源发布。
- 上游 LabelImg2 的 MIT 版权与许可证声明单独原样保存在 `LICENSE-MIT-UPSTREAM`。
