# 本 Fork 的功能改动

本文档总结相对于上游 `chinakook/labelImg2` 的主要变化。

## 更便捷的 OBB 标注流程

- `E` 可以进入或退出旋转框绘制状态。
- 取消容易误触的 `W` 普通框快捷键。
- 每画完一个框自动返回编辑状态，不会不停进入下一个绘制状态。
- 新框自动选中并打开类别编辑器，可直接输入类别首字母。
- 类别编辑期间禁用图片切换快捷键，防止输入 `d` 时误切图片。

## 更快的类别选择

- `set as default` 设置的类别可以跨重启保存。
- 相同首字母的类别按照使用频率和最近使用顺序排列。
- 1.2 秒内连续按同一个首字母，可以在匹配类别中循环。
- 类别使用统计保存在本机设置文件，不会写入标注数据。

## 更统一的鼠标操作

- 没有选中框时，滚轮缩放图片。
- 有选中框时，滚轮缩放选中框，不会同时缩放图片。
- `Alt + 左键拖动`平移画布。
- 图片空白处拖动可以框选多个 Box。
- `Ctrl/Shift + 框选`可以追加选择。
- 多个选中框可以集体移动、缩放、复制和删除。

## 复制粘贴和重复目标标注

- 支持 `Ctrl+C`、`Ctrl+V` 和右键复制粘贴。
- 支持 `Ctrl+D`批量复制选中框。
- 支持 `Ctrl + 拖动 Box`直接复制并移动。
- 同图粘贴自动偏移，避免框完全重叠。
- 跨图片粘贴保留原坐标、类别、大小和角度。

## 图片浏览和进度恢复

- `A/D` 和左右方向键切换图片。
- 图片按照自然数字顺序排列。
- 自动恢复上次图片目录和具体标注位置。
- 自动恢复 XML 保存目录、默认类别和类别使用顺序。
- Change Dir 不再覆盖用户明确选择的标签保存目录。

## 保存和稳定性

- 保存 XML 前自动创建缺少的相对子目录。
- 图片读取失败时显示错误，不继续使用无效画布。
- 空 Pixmap、无效尺寸和异常缩放值增加保护。

## YOLO/YOLO OBB 导出

- 没有同名 XML 时可直接读取标准 YOLO OBB `.txt` 标签。
- TXT 按图片目录的相对子目录结构递归匹配。
- 右侧文件列表统计有效 TXT 标签行数，类别 ID 按当前类别预设顺序映射。
- 编辑导入的 TXT 标签后保存为 Pascal VOC XML，不覆盖源 TXT。
- XML 直接转换为同名 YOLO 或 YOLO OBB `.txt`。
- 普通 YOLO 使用中心点和宽高格式。
- YOLO OBB 使用归一化四顶点格式。
- 不复制图片。
- 不重复生成 train 和 val 数据。
- 不创建数据集划分目录。
- 不生成 YAML、列表或其他附加文件。
- 类别 ID 按当前类别预设顺序确定。
